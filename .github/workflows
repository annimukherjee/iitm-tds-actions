name: IITMTDS

on: push

# This block ensures that only one run per branch (or workflow) is active.
# When a new run is triggered, any in-progress run in the same concurrency group is canceled.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f1003186@ds.study.iitm.ac.in
        run: echo "Hello, world!"
